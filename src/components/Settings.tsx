import { FC } from 'react';
import { Classes } from '@blueprintjs/core';

const Settings: FC = () => {
  return (
    <div className={Classes.DRAWER_BODY}>
      <div className={Classes.DIALOG_BODY}>Settings</div>
    </div>
  );
};

export default Settings;
